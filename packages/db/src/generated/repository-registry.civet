import type { DatabaseExecutor } from "../server/client.civet"

// Generator-owned. Edit through `pnpm generate:feature` only.
export type GeneratedFeatureRepositories = Readonly<Record<never, never>>

export const createGeneratedFeatureRepositories = (
  _database: DatabaseExecutor,
): GeneratedFeatureRepositories => Object.freeze({})
