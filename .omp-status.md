# OMP Status

- Updated: 2026-07-24T06:26:28Z
- Session: DarkFactory asynchronous verification and publication
- Goal: Publish the implemented foundation without recreating the synchronous Graphify/browser traffic jam, while preserving every required gate as an observable CI lane.
- Branch: `main`; the working tree contains the verification-lane scheduling change on top of local foundation commits `c9185b7` and `e40a04f`.
- Changed scope: Full monorepo foundation plus a lifecycle-only follow-up across root scripts, Husky pre-push, GitHub Actions, exact contract tests, generated architecture inventory, and operator/specification documentation.
- Completed: Implemented the 16-package foundation and split verification into exact core, integration, graph, and browser lanes. `pnpm verify` and `pnpm run ci` still compose all four; pre-push runs the broad deterministic core lane; GitHub Actions runs every lane concurrently with `fail-fast: false`, isolated dependencies, cleanup, and browser failure evidence.
- Last verification: PASS — `verify:core`, including format/Markdown lint, Better Auth schema, OpenAPI freshness, full typecheck with 62 exported type targets, five-phase build, 13/13 unit tasks, 79/79 contract tests, 231/231 operations/E2E-helper tests, and current generated inventory for 16 packages; isolated `verify:integration` 96/96 with PostgreSQL removed afterward; lifecycle contract 4/4; workflow YAML parsed with the exact four-lane matrix; `git diff --check`; terminal security review APPROVE and terminal code review APPROVE after all findings were closed.
- Evidence boundary: Local Graphify refresh/query work and local browser/visual reruns remain stopped per the user direction. Their required checks were not suppressed: they are isolated `Verification (graph)` and `Verification (browser)` CI lanes, and no freshness or browser acceptance will be claimed until those exact remote results are terminal. The earlier browser investigation remains safely summarized without raw messages, stacks, URLs, secrets, credentials, or personal data.
- Next action: Commit the reviewed lane refactor, push normally through the passing core hook without bypassing it, and follow all four GitHub Actions lanes to terminal states. Record exact remote evidence or a bounded blocker rather than reopening a serial local loop.
- Stop condition: Publication is complete only when the reviewed commits are remote and all four CI lanes are terminal; only an all-green matrix proves the full lifecycle.

## Prioritized follow-ups

- [ ] **P1 — Shannon security hardening.** Source: [KeygraphHQ/shannon](https://github.com/KeygraphHQ/shannon). Before execution, consult the current official instructions and approve a staging-only threat model/baseline, isolated source copy, isolated non-production target, written rules of engagement, scoped least-privilege test identities and credentials, data handling, human stop authority, and immediate stop conditions. Shannon is authorized only for white-box, source-guided live exploitation within that scope; never use it for black-box testing or production. Acceptance requires inspectable coverage of authentication, authorization, sessions, cookies, input validation, SSRF, injection, rate limiting, security headers, CSRF, secret exposure, dependency risk, and PostgreSQL least privilege. Findings must have severity, reproducible redacted evidence, ownership, remediation, regression tests, a successful rebuild, and a focused scoped rerun proving closure. Never retain raw credentials, tokens, cookies, personal data, or provider payloads.
- [ ] **P2 — AI-agent SDLC and loop engineering.** Source: [VQy50fuxI34](https://www.youtube.com/watch?v=VQy50fuxI34). Convert the existing workflow into an inspectable loop of spec/contract → failure-reproducing eval or test → narrow reviewed tool allowlist → bounded execution → observability and redacted evidence → independent review → CI → explicit stop criteria and feedback. Capture the failing eval before changing the harness or process. Before each subsequent iteration, deliberately refresh only the required context, record narrow tool and filesystem/network permissions plus human-escalation boundaries, and write a disk checkpoint with inputs, state, owner, and tested recovery steps. Acceptance requires one representative change to demonstrate deterministic inputs, resource/time limits, bounded retries and terminal stop criteria, failure/retry/recovery behavior, durable evidence linked to the exact revision, explicit permission or human escalation for out-of-scope actions, reviewer disposition, terminal CI result, and a feedback record that updates the next spec/eval without silently widening scope.
