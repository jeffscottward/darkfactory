# OMP Status

- Updated: 2026-07-28T00:57:53Z
- Session: DarkFactory v0.2.0 release-candidate execution resumed
- Goal: Commit the release metadata/configuration on top of green base `2eaa5f7`, pass the release PR gates, merge and record the resulting exact SHA, deploy and verify that same SHA in production, and only then publish v0.2.0 before completing the retained domain migration work.
- Branch: `release/v0.2.0` is based on pushed green candidate `2eaa5f7`. That commit is the green base, not the future merge, deployment, tag, or release SHA.
- Changed scope: the intentional release-candidate modifications are `.gitignore`, `.graphify/manifest.json`, `.markdownlint-cli2.jsonc`, `.omp-status.md`, `CHANGELOG.md`, `package.json`, `apps/web/package.json`, all 14 `packages/*/package.json` workspace manifests, `apps/web/wrangler.jsonc`, `capabilities.yaml`, `packages/config/src/capabilities.civet`, `packages/config/src/capabilities.test.civet`, `packages/api/src/openapi.civet`, `packages/api/openapi.json`, and `docs/generated/architecture-inventory.json`. The release metadata moves every governed service declaration to `0.2.0`; the tracked Graphify manifest records the verified 631-file source fingerprint; the Markdown rule permits repeated changelog categories only across independent sections; the Wrangler configuration adds explicit production and preserved staging contracts without secrets. Dependency ranges, historical v0.1.0 evidence, unrelated fixtures, and lockfile dependency versions remain unchanged. Shannon remains optional future hardening. The Cloudflare nameserver migration and Namecheap-to-Cloudflare registrar transfer remain retained; asynchronous state must not be represented as complete before independent confirmation.
- Completed: aggregate `bun run verify` passed against the final diff, covering core, verified 100% coverage, real-Postgres integration, refreshed Graphify check/verify, Chromium E2E, and accessibility; production and staging deploy dry-runs also passed. The production PlanetScale app role was recreated with fresh credentials, its connection was verified, the existing five-entry migration ledger and 12 public tables were confirmed, obsolete exposed and Shannon-only roles were revoked, and the production Worker secret bundle was prepared in an ignored mode-0600 recovery artifact. Namecheap accepted the Cloudflare nameserver change and Cloudflare was asked to recheck it.
- Last verification: final local `bun run verify`, both deploy dry-runs, the production dependency audit, and `git diff --check` all exit 0; the dependency audit reports no known vulnerabilities. GitHub CI, CodeQL, and OpenSSF Scorecard remain green for base `2eaa5f7`. The production URL contract is `https://darkfactory.jeffscott.world`. The authoritative nameserver mutation was submitted, but three public resolvers still returned the prior Namecheap servers at the last readback, so propagation remains pending. Production Worker deployment, the four production smoke flows, Cloudflare zone activation, email-routing parity, registrar transfer, and the v0.2.0 release are not yet complete and are not claimed as complete.
- Next action: commit and push the metadata/configuration → open the release PR → wait for every required check, including PR-only Dependency Review → merge and record the exact merged SHA → deploy that same SHA to production → smoke-test the public page, authentication, authenticated portal, and one persisted database write/read → tag and publish v0.2.0 from that same verified SHA. In parallel, confirm nameserver propagation and DNS/mail parity before initiating or continuing the retained registrar transfer.
- Stop condition: one recorded merged SHA has passed all PR gates, is deployed at `https://darkfactory.jeffscott.world`, passes all four production smoke flows, and is the exact SHA tagged and published as v0.2.0. Preserve DNS parity evidence and track the Cloudflare nameserver switch plus registrar transfer to confirmed completion without making asynchronous transfer completion a release prerequisite.

## Current checkpoint

- Goal tracker: execution resumed for the bounded v0.2.0 release path. The active order is metadata/configuration commit → release PR → all checks including PR-only Dependency Review → merge and record exact SHA → production deploy of that SHA → public/authentication/portal/database smoke → tag and release that same SHA. No later step may be claimed before its evidence exists.
- Coverage target: complete locally. The V8 gate reports 100% statements, 100% branches, 100% functions, and 100% lines—12,164/12,164 statements, 7,967/7,967 branches, 2,307/2,307 functions, and 11,000/11,000 lines. No authored runtime-source exclusions or ignore annotations were added.
- Agent Friendly target: retain truthful 98.7. Jeff explicitly accepted the manageable-size heuristic shortfall because the repository's Graphify graph provides scoped navigation; do not split, consolidate, ignore, or delete files merely to change this score.
- OpenSSF target: official Best Practices Passing already reports 100%, which is the requested result. The GitHub OpenSSF Scorecard check for `2eaa5f7` is green; do not pursue Silver/Gold or `access_continuity` as a release prerequisite.
- Deployment, release, and domain path: the prepared Cloudflare Worker and PlanetScale Postgres path remains the deployment route, with `https://darkfactory.jeffscott.world` as the production URL contract. The least-privilege production app role and ignored Worker secret bundle are ready, while the production Worker does not yet exist. Merge and record one reviewed immutable SHA, deploy and verify that same SHA through the four named production flows, then tag and publish it as v0.2.0. The Cloudflare zone retains the apex and `www` records DNS-only for parity; the authoritative nameserver change has been submitted and remains pending independent propagation confirmation. Verify DNS and mail behavior before retaining the registrar transfer to confirmed completion.
- Shannon status: the recovery-only Groq/provider, gateway, broker, redaction, and artifact work is preserved for optional future review. No live Shannon scan completed, no Shannon badge is supported, and no Shannon result may be represented as release evidence. Do not resume this work unless it is separately prioritized after the application is deployed.

## Prioritized follow-ups

- [ ] **Deferred — optional Shannon security hardening.** Source: [KeygraphHQ/shannon](https://github.com/KeygraphHQ/shannon). This is not a release gate. Reconsider only after the application is deployed and only with a separate time budget and explicit acceptance criteria. If resumed, use disposable non-production staging, synthetic least-privilege identities, written rules of engagement, bounded cost/rate/time limits, redacted evidence, human stop authority, and immediate cleanup. Never test production or imply a badge/result without a completed supported assessment.
- [ ] **P2 — AI-agent SDLC and loop engineering.** Source: [VQy50fuxI34](https://www.youtube.com/watch?v=VQy50fuxI34). Convert the existing workflow into an inspectable loop of spec/contract → failure-reproducing eval or test → narrow reviewed tool allowlist → bounded execution → observability and redacted evidence → independent review → CI → explicit stop criteria and feedback. Capture the failing eval before changing the harness or process. Before each subsequent iteration, deliberately refresh only the required context, record narrow tool and filesystem/network permissions plus human-escalation boundaries, and write a disk checkpoint with inputs, state, owner, and tested recovery steps. Acceptance requires one representative change to demonstrate deterministic reruns, bounded retries and budgets, clean cancellation and recovery, evidence retention, reviewer independence, and a measurable feedback update to the next iteration.

## Final repository-health polish — run last

- [x] **Install the supplied README banner.** The exact supplied 1568×523 WebP is already preserved at `docs/assets/darkfactory-banner.webp` (155,570 bytes; SHA-256 `1bf4ce057dd4a68a02221be9cc541873de1df2fabcf05c6d5b60a8da7589f057`). As part of the final README polish, place this repository-owned asset above the title with accurate alt text and responsive rendering; do not hotlink, regenerate, crop, or distort it.
- [x] **Build a near-top repository-health badge table.** Use truthful, live, linked badges for CI/build status, test coverage, latest semantic-version release, code license, “PRs welcome,” open issue count, and open pull-request count. Add other meaningful signals only when backed by an authoritative source—good candidates include CodeQL/security, dependency review or update health, OpenSSF Scorecard, accessibility/E2E status, supported Node and pnpm versions, last release/date, contributors, and repository activity. Avoid vanity, duplicate, stale, misleading, or manually fabricated metrics; verify every image and target URL.
- [x] **Assess and optimize Agent Friendly Code.** Source: [agentfriendlycode.com](https://www.agentfriendlycode.com/). After all other repository work is complete and green, consult the current official assessment method, capture a reproducible baseline, fix substantive repository issues without gaming or weakening safeguards, rerun until a perfect score is reached or a precise external blocker is documented, retain inspectable evidence, and add the official score/status badge linked to that evidence if the service supports one.
- [x] **Assess and optimize OpenSSF Best Practices.** Source: [bestpractices.dev](https://www.bestpractices.dev/). Run this only after the Agent Friendly Code pass. Use the current official project enrollment and criteria, answer claims truthfully with repository evidence, close every applicable gap needed for the highest defensible badge level, document any non-applicable or externally blocked criterion, and add the official badge linked to the public project assessment.
- [x] **Verify and publish the final health polish.** Re-run all local and GitHub core, integration, graph, browser, accessibility, coverage, security, and documentation gates after the banner, badge table, and assessment-driven changes. Confirm the README renders correctly at desktop and mobile widths, all badges resolve and report current facts, the repository is clean, and the final public revision has every configured check green.
