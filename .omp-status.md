# OMP Status

- Updated: 2026-07-26T07:53:47Z
- Session: DarkFactory release follow-through — CI portability repair and isolated staging provisioning
- Goal: Repair the two CI-only portability regressions without weakening security, publish a new exact green candidate, deploy that immutable SHA to isolated staging, pass the authorized Shannon gate, then release and promote it.
- Branch: `main` is synchronized through pushed release-candidate commit `a2ac8c8`; the first GitHub run exposed bounded Portless startup/cleanup and cross-platform Graphify identity regressions. The locally committed surgical follow-up is green and independently approved; its first push was correctly rejected because the hook inherited a non-maintenance `DATABASE_URL`, so the retry must supply the isolated E2E maintenance URL.
- Changed scope: Jeff accepted Agent Friendly 98.7, defined OpenSSF as the earned Best Practices 100%, authorized moving `jeffscott.world` from Namecheap to Cloudflare, selected Bun as the primary runtime/badge, required mirrored repository `AGENTS.md`/`CLAUDE.md`, required complete prerequisite automation, required honest 100% measured coverage without exclusions or weakened assertions, and deferred feature E2E work while retaining the browser smoke gate.
- Completed: the follow-up uses bounded browser-local Portless startup/readiness/termination proof; portable capability plus descriptor-pinned cryptographic ownership checks; a private root-owned system-temp Graphify snapshot namespace with quarantine, bounded cleanup, and primary-error-preserving rollback; local/test host environment preservation without secret exposure; and explicit matching CLI confirmation for development database seed/reset so dotenv alone cannot authorize destructive lifecycle commands. Isolated PlanetScale Postgres, least-privilege staging credentials, Cloudflare Worker configuration, and five Worker secrets remain prepared; Cloudflare free-zone onboarding is paused at the nameserver instructions without traffic changes.
- Last verification: `bun run verify:core` passed 53 files and 903 tests; `bun run verify:integration` passed 13 files and 97 tests with developer `.dev.vars` isolated and restored; `bun run test:coverage` passed 164 files and 2,710 tests at exactly 100% statements, branches, functions, and lines; the focused database CLI unit suite passed 25/25; the focused database lifecycle integration suite passed 2/2; documented Bun confirmation syntax rejected a mismatched environment before database access; coverage, Graphify (610 sources/five queries), documentation, Wrangler staging dry-run, and secret-name-only inspection checks are current and green.
- Next action: amend this checkpoint, push the exact locally green candidate with the isolated E2E maintenance URL supplied to the hook, monitor every configured GitHub check to terminal green, then deploy that immutable SHA to the prepared staging Worker and execute the bounded Shannon gate.
- Stop condition: all requested tooling changes and an honest 100% four-metric coverage gate pass; the exact candidate passes isolated staging and Shannon; that SHA is released and promoted; all records and redirect behavior survive Cloudflare nameserver/registrar consolidation; final CI and production probes are green.

## Current checkpoint

- Goal tracker: coverage, OpenSSF, tooling alignment, README health, foundation hardening, and local CI portability repairs are complete. Release remains active: the exact follow-up candidate passed terminal code and security review and awaits push; isolated staging data, secrets, and configuration are prepared but the application is intentionally not deployed until that SHA is green; Shannon remains blocked on that deployment.
- Coverage target: complete. The final configured V8 gate reports 100% statements, 100% branches, 100% functions, and 100% lines across 2,710 passing tests, with no coverage exclusions, ignore annotations, repeated-call gaming, or weakened assertions added to manufacture the result.
- Agent Friendly target: retain truthful 98.7. Jeff explicitly accepted the manageable-size heuristic shortfall because the repository's Graphify graph provides scoped navigation; do not split, consolidate, ignore, or delete files merely to change this score.
- OpenSSF target: official Best Practices Passing already reports 100%, which is the requested result. Remove the distinct OpenSSF Scorecard `/10` badge/presentation rather than treating its 7.1 as this goal. Do not pursue Silver/Gold or `access_continuity`.
- Deployment, release, and domain path: deploy an exact green candidate SHA—not a final tag—to the prepared `darkfactory-web-staging` Cloudflare Worker backed by disposable PlanetScale Postgres. Staging schema migration, a least-privilege application role, five Worker secrets, and production-mode environment configuration are complete; direct Worker-to-Postgres compatibility and outbound Resend delivery must be proved by the deployed smoke test. After Shannon passes, tag/release and promote that immutable SHA. Exact private Namecheap settings, public DNS answers, and Cloudflare's scanned records are stored under `/Users/jeffscottward/Github/glen-ross/.session-recovery/darkfactory/domain-consolidation/` with owner-only permissions. Cloudflare zone onboarding is waiting at nameserver activation; Namecheap authentication is the remaining cutover blocker, and no nameserver, registrar, DNSSEC, billing, or traffic-affecting change has occurred. Reproduce the URL redirect explicitly and verify email/web behavior before nameserver cutover; transfer the registrar only after the Cloudflare zone is active and stable.
- Shannon gate: repository policy forbids Shannon against production. Update the existing `/Users/jeffscottward/Github/tools/shannon` clone only after isolated staging exists, then run and rerun authorized white-box testing solely against that disposable target with written scope, synthetic identities, no production/customer data or credentials, restricted egress, safe rate/concurrency limits, and immediate stop authority. Remediate every accepted finding before tagging; add no badge unless the evidence supports one.

## Prioritized follow-ups

- [ ] **P1 — Shannon security hardening.** Source: [KeygraphHQ/shannon](https://github.com/KeygraphHQ/shannon). Before execution, consult the current official instructions and approve a staging-only threat model/baseline, isolated source copy, isolated non-production target, written rules of engagement, scoped least-privilege test identities and credentials, data handling, human stop authority, and immediate stop conditions. Shannon is authorized only for white-box, source-guided live exploitation within that scope; never use it for black-box testing or production. Acceptance requires inspectable coverage of authentication, authorization, sessions, cookies, input validation, SSRF, injection, rate limiting, security headers, CSRF, secret exposure, dependency and configuration risk, plus a reviewed remediation backlog with regression tests.
- [ ] **P2 — AI-agent SDLC and loop engineering.** Source: [VQy50fuxI34](https://www.youtube.com/watch?v=VQy50fuxI34). Convert the existing workflow into an inspectable loop of spec/contract → failure-reproducing eval or test → narrow reviewed tool allowlist → bounded execution → observability and redacted evidence → independent review → CI → explicit stop criteria and feedback. Capture the failing eval before changing the harness or process. Before each subsequent iteration, deliberately refresh only the required context, record narrow tool and filesystem/network permissions plus human-escalation boundaries, and write a disk checkpoint with inputs, state, owner, and tested recovery steps. Acceptance requires one representative change to demonstrate deterministic reruns, bounded retries and budgets, clean cancellation and recovery, evidence retention, reviewer independence, and a measurable feedback update to the next iteration.

## Final repository-health polish — run last

- [x] **Install the supplied README banner.** The exact supplied 1568×523 WebP is already preserved at `docs/assets/darkfactory-banner.webp` (155,570 bytes; SHA-256 `1bf4ce057dd4a68a02221be9cc541873de1df2fabcf05c6d5b60a8da7589f057`). As part of the final README polish, place this repository-owned asset above the title with accurate alt text and responsive rendering; do not hotlink, regenerate, crop, or distort it.
- [x] **Build a near-top repository-health badge table.** Use truthful, live, linked badges for CI/build status, test coverage, latest semantic-version release, code license, “PRs welcome,” open issue count, and open pull-request count. Add other meaningful signals only when backed by an authoritative source—good candidates include CodeQL/security, dependency review or update health, OpenSSF Scorecard, accessibility/E2E status, supported Node and pnpm versions, last release/date, contributors, and repository activity. Avoid vanity, duplicate, stale, misleading, or manually fabricated metrics; verify every image and target URL.
- [x] **Assess and optimize Agent Friendly Code.** Source: [agentfriendlycode.com](https://www.agentfriendlycode.com/). After all other repository work is complete and green, consult the current official assessment method, capture a reproducible baseline, fix substantive repository issues without gaming or weakening safeguards, rerun until a perfect score is reached or a precise external blocker is documented, retain inspectable evidence, and add the official score/status badge linked to that evidence if the service supports one.
- [x] **Assess and optimize OpenSSF Best Practices.** Source: [bestpractices.dev](https://www.bestpractices.dev/). Run this only after the Agent Friendly Code pass. Use the current official project enrollment and criteria, answer claims truthfully with repository evidence, close every applicable gap needed for the highest defensible badge level, document any non-applicable or externally blocked criterion, and add the official badge linked to the public project assessment.
- [x] **Verify and publish the final health polish.** Re-run all local and GitHub core, integration, graph, browser, accessibility, coverage, security, and documentation gates after the banner, badge table, and assessment-driven changes. Confirm the README renders correctly at desktop and mobile widths, all badges resolve and report current facts, the repository is clean, and the final public revision has every configured check green.
