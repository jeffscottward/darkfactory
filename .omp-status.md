# OMP Status

- Updated: 2026-07-24T06:01:39Z
- Session: DarkFactory full-foundation implementation — static verification and publication handoff
- Goal: Publish the implemented foundation with truthful verification evidence while keeping the user-deferred Graphify and browser investigation out of the critical path.
- Branch: `main`; final worktree is verified but not yet committed or pushed.
- Changed scope: Full monorepo foundation across web experience, auth, PostgreSQL, oRPC, providers, lifecycle tooling, generated contracts/inventory, tests, CI, and operator documentation.
- Completed: Implemented the 16-package foundation, Squarespace-inspired public experience, shadcn-inspired authenticated portal, Better Auth/PostgreSQL account flows, contract-first oRPC services, capability adapters, local HTTPS tooling, deterministic generators, hardened E2E lifecycle/evidence tooling, and durable architecture/agent guidance.
- Last verification: PASS — format and Markdown lint; Better Auth schema; OpenAPI freshness; full typecheck from a declaration-clean state with 62 exported type targets; five-phase Vinext build; unit tasks 13/13; contract tests 79/79; operations/E2E-helper tests 230/230; PostgreSQL integration tests 96/96; generated documentation inventory current for 16 packages; `git diff --check`; terminal code and security reviews with no P0-P3 findings. The isolated PostgreSQL service was removed after verification.
- Evidence boundary: Graphify refresh and queries were explicitly deferred by the user; no graph freshness is claimed. Browser/visual acceptance was also stopped rather than allowed to block independent work. The unresolved account journey observed four identical account-page errors using safe diagnostics only: category `unknown`, name `Error`, route class `account/[section]`, source class `app`, and source line `40`. Coordination owner: `Main`, who must assign the eventual product/runtime owner. Rerun only after a static root-cause fix, targeted tests, and fresh reviews. The canonical runner currently aggregates ordinary failures and has no proven selector/fail-fast contract, so implement and test that contract first or explicitly authorize the complete serialized suite; never claim a focused or `maxFailures=1` run today. Any authorized run must use an isolated database, safe classifier fields, and verified process/database/artifact cleanup. No raw message, stack, URL, secret, credential, or personal data is retained here.
- Next action: Commit and push the verified non-browser state, monitor the actual CI result, and record any Graphify/browser-required CI blocker without reopening the synchronous diagnostic loop. Execute the P1/P2 follow-ups below only under their stated controls.
- Stop condition: This handoff is complete when the commit is pushed and its CI result is recorded truthfully. Do not claim final browser acceptance or graph freshness until those separately authorized gates pass on the exact revision.

## Prioritized follow-ups

- [ ] **P1 — Shannon security hardening.** Source: [KeygraphHQ/shannon](https://github.com/KeygraphHQ/shannon). Before execution, consult the current official instructions and approve a staging-only threat model/baseline, isolated source copy, isolated non-production target, written rules of engagement, scoped least-privilege test identities and credentials, data handling, human stop authority, and immediate stop conditions. Shannon is authorized only for white-box, source-guided live exploitation within that scope; never use it for black-box testing or production. Acceptance requires inspectable coverage of authentication, authorization, sessions, cookies, input validation, SSRF, injection, rate limiting, security headers, CSRF, secret exposure, dependency risk, and PostgreSQL least privilege. Findings must have severity, reproducible redacted evidence, ownership, remediation, regression tests, a successful rebuild, and a focused scoped rerun proving closure. Never retain raw credentials, tokens, cookies, personal data, or provider payloads.
- [ ] **P2 — AI-agent SDLC and loop engineering.** Source: [VQy50fuxI34](https://www.youtube.com/watch?v=VQy50fuxI34). Convert the existing workflow into an inspectable loop of spec/contract → failure-reproducing eval or test → narrow reviewed tool allowlist → bounded execution → observability and redacted evidence → independent review → CI → explicit stop criteria and feedback. Capture the failing eval before changing the harness or process. Before each subsequent iteration, deliberately refresh only the required context, record narrow tool and filesystem/network permissions plus human-escalation boundaries, and write a disk checkpoint with inputs, state, owner, and tested recovery steps. Acceptance requires one representative change to demonstrate deterministic inputs, resource/time limits, bounded retries and terminal stop criteria, failure/retry/recovery behavior, durable evidence linked to the exact revision, explicit permission or human escalation for out-of-scope actions, reviewer disposition, terminal CI result, and a feedback record that updates the next spec/eval without silently widening scope.
