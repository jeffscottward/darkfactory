# OMP Status

- Updated: 2026-07-28T03:15:36Z
- Session: DarkFactory v0.2.0 release-blocker diagnosis complete; immutable release execution resumed
- Goal: make one final reviewed commit, require fresh CI on its exact SHA, use one guarded owner-admin squash merge for the otherwise-unsatisfiable review rule, deploy the returned merge SHA, verify production, and only then tag and publish v0.2.0 from that same SHA.
- Branch: `release/v0.2.0`; local commit `7f0580b` contains the reviewed fail-closed staging-email patch and is one commit ahead of pushed head `0ae6a2f`. The first push attempt stopped safely in the mandatory pre-push hook before any remote update. PR #7 remains conflict-free and code-mergeable; its remote blocker is still `REVIEW_REQUIRED`.
- Changed scope: the final patch contains `.env.schema`, `.graphify/manifest.json`, `.omp-status.md`, `apps/web/wrangler.jsonc`, the config environment source and test, and the email public option declarations, auth/contact selector sources, and tests. Production alone retains Resend delivery and `no-reply@send.darkfactory.jeffscott.world`; staging now uses an explicit disabled provider/transport pair, an inert `.test` From address, and no Resend credential. The tracked 619-file graph manifest records this post-checkpoint source state, and its final refresh and verification passed.
- Completed: both duplicate CI workflow matrices, CodeQL, action and JavaScript analysis, Dependency Review, and the Graphify jobs are green at `0ae6a2f`. Cloudflare Email Routing is enabled, ready, and synced with a verified catch-all destination; both authorities publish Cloudflare MX, SPF, and DKIM records, and a non-self Resend message to a never-used local part arrived after the former 30-minute TTL window with the alias preserved in Gmail headers. The dedicated Resend sender domain is verified. A new isolated PlanetScale `darkfactory-production` Postgres 18.4 PS-5 database is ready in `us-east-1`, has all five migrations and 12 public tables, and the least-privilege app role passed read plus transactional write/rollback probes. All production bundle values implicated by session output were rotated; the old Resend key, old staging-hosted database role, exposed intermediate password, and ephemeral migration role were invalidated, the migration credential file was removed, and production console access was disabled.
- Last verification: the focused disabled-email suite reports 3 files and 115 tests passed; declaration generation and full typecheck pass; `bun run verify:core` passed before commit, including 54 operations files and 1,005 tests; an isolated `bun run test:operations` rerun after the stopped push again passed all 1,005 tests; `bun run verify:graph` resolves all five queries against the 619-file corpus; and `bun run deploy:web:check` exits 0. The first pre-push hook inherited the developer `.env` database URL through its outer Bun process and correctly rejected it as an unsafe E2E maintenance database; no remote update occurred. GitHub still reports every prior check green at `0ae6a2f`.
- Next action: amend this checkpoint into the reviewed commit, then retry the push once with the canonical local-only E2E maintenance database URL supplied to the outer pre-push process so Bun cannot preload the developer database URL into the discovery-only config test. Require the complete pre-push `verify:core` gate to pass, capture the final head SHA, and require fresh 14/14 checks on that unchanged SHA. Reconfirm that no conversation is unresolved and the impossible one-person approval is the sole unmet rule, then run one SHA-guarded owner-admin squash merge without weakening branch protection. Record the returned merge SHA, require its tree to equal the final branch tree, wait for main checks, build from a clean detached worktree, and deploy that SHA with the mode-0600 secrets file. Verify all named production flows before creating the annotated tag and GitHub release.
- Stop condition: one recorded merged SHA has passed every configured automated gate, is deployed at `https://darkfactory.jeffscott.world`, passes all named production smoke flows, and is the exact SHA tagged and published as v0.2.0. Preserve DNS/mail evidence and retain registrar transfer as a separate follow-through.

## Current checkpoint

- Five-minute alignment (2026-07-28T03:15:36Z): the reviewed commit exists locally, and the first push stopped before remote mutation because the outer Bun hook preloaded the developer database URL into a discovery-only E2E config test. The same 1,005-test operations suite passes with a clean inherited environment. This is a bounded invocation-environment issue, not a product-code regression; the immediate next action is one checkpoint amend and one push retry under the canonical local-only test database contract. No optional scope was added.
- Coverage target: complete locally. The V8 gate reports 100% statements, 100% branches, 100% functions, and 100% lines—12,164/12,164 statements, 7,967/7,967 branches, 2,307/2,307 functions, and 11,000/11,000 lines. No authored runtime-source exclusions or ignore annotations were added.
- Agent Friendly target: retain truthful 98.7. Jeff explicitly accepted the manageable-size heuristic shortfall because the repository's Graphify graph provides scoped navigation; do not split, consolidate, ignore, or delete files merely to change this score.
- OpenSSF target: official Best Practices Passing already reports 100%, which is the requested result. The GitHub OpenSSF Scorecard check for `2eaa5f7` is green; do not pursue Silver/Gold or `access_continuity` as a release prerequisite.
- Deployment, release, and domain path: `https://darkfactory.jeffscott.world` remains the production URL contract. The isolated production database is migrated and verified, the app role is least-privilege, every exposed credential was invalidated, the replacement Worker bundle is ignored and mode 0600, Email Routing passed a non-self end-to-end catch-all test, and the Resend sender subdomain is verified. The production Worker still does not exist, so the first deployment has no rollback version; do not deploy before the final merged SHA is recorded. After exact-SHA deployment, create and verify the production smoke account, exercise one persisted write/read, then tag and publish v0.2.0. The registrar transfer remains independent follow-through.
- Release and credential checkpoint: `semantic-release` is absent from dependencies, configuration, scripts, lockfile, and workflows; Semantic Versioning is only the numbering policy. The reporting-only `_dmarc.send.darkfactory.jeffscott.world` policy is publicly visible through Cloudflare and Google resolvers. Production alone has the verified-domain Resend key and sender identity in the mode-0600 deployment bundle; staging has no Resend secret, its temporary key was revoked, and disabled delivery fails closed.
- Shannon status: the recovery-only Groq/provider, gateway, broker, redaction, and artifact work is preserved for optional future review. No live Shannon scan completed, no Shannon badge is supported, and no Shannon result may be represented as release evidence. Do not resume this work unless it is separately prioritized after the application is deployed.

## Prioritized follow-ups

- [ ] **Deferred — optional Shannon security hardening.** Source: [KeygraphHQ/shannon](https://github.com/KeygraphHQ/shannon). This is not a release gate. Reconsider only after the application is deployed and only with a separate time budget and explicit acceptance criteria. If resumed, use disposable non-production staging, synthetic least-privilege identities, written rules of engagement, bounded cost/rate/time limits, redacted evidence, human stop authority, and immediate cleanup. Never test production or imply a badge/result without a completed supported assessment.
- [ ] **P2 — AI-agent SDLC and loop engineering.** Source: [VQy50fuxI34](https://www.youtube.com/watch?v=VQy50fuxI34). Convert the existing workflow into an inspectable loop of spec/contract → failure-reproducing eval or test → narrow reviewed tool allowlist → bounded execution → observability and redacted evidence → independent review → CI → explicit stop criteria and feedback. Capture the failing eval before changing the harness or process. Before each subsequent iteration, deliberately refresh only the required context, record narrow tool and filesystem/network permissions plus human-escalation boundaries, and write a disk checkpoint with inputs, state, owner, and tested recovery steps. Acceptance requires one representative change to demonstrate deterministic reruns, bounded retries and budgets, clean cancellation and recovery, evidence retention, reviewer independence, and a measurable feedback update to the next iteration.
- [ ] **P2 — Broader DMARC enforcement after mail stabilization.** Reporting-only DMARC is now live for the dedicated `send.darkfactory.jeffscott.world` sender domain with aggregate reports routed through the verified catch-all. Retain this follow-up for the apex `jeffscott.world` policy and for deliberate progression from `p=none` to quarantine/reject only after every legitimate sender is aligned and reports have been reviewed. This is not a v0.2.0 deployment gate.

## Final repository-health polish — run last

- [x] **Install the supplied README banner.** The exact supplied 1568×523 WebP is already preserved at `docs/assets/darkfactory-banner.webp` (155,570 bytes; SHA-256 `1bf4ce057dd4a68a02221be9cc541873de1df2fabcf05c6d5b60a8da7589f057`). As part of the final README polish, place this repository-owned asset above the title with accurate alt text and responsive rendering; do not hotlink, regenerate, crop, or distort it.
- [x] **Build a near-top repository-health badge table.** Use truthful, live, linked badges for CI/build status, test coverage, latest semantic-version release, code license, “PRs welcome,” open issue count, and open pull-request count. Add other meaningful signals only when backed by an authoritative source—good candidates include CodeQL/security, dependency review or update health, OpenSSF Scorecard, accessibility/E2E status, supported Node and pnpm versions, last release/date, contributors, and repository activity. Avoid vanity, duplicate, stale, misleading, or manually fabricated metrics; verify every image and target URL.
- [x] **Assess and optimize Agent Friendly Code.** Source: [agentfriendlycode.com](https://www.agentfriendlycode.com/). After all other repository work is complete and green, consult the current official assessment method, capture a reproducible baseline, fix substantive repository issues without gaming or weakening safeguards, rerun until a perfect score is reached or a precise external blocker is documented, retain inspectable evidence, and add the official score/status badge linked to that evidence if the service supports one.
- [x] **Assess and optimize OpenSSF Best Practices.** Source: [bestpractices.dev](https://www.bestpractices.dev/). Run this only after the Agent Friendly Code pass. Use the current official project enrollment and criteria, answer claims truthfully with repository evidence, close every applicable gap needed for the highest defensible badge level, document any non-applicable or externally blocked criterion, and add the official badge linked to the public project assessment.
- [x] **Verify and publish the final health polish.** Re-run all local and GitHub core, integration, graph, browser, accessibility, coverage, security, and documentation gates after the banner, badge table, and assessment-driven changes. Confirm the README renders correctly at desktop and mobile widths, all badges resolve and report current facts, the repository is clean, and the final public revision has every configured check green.
