import { PageHeader } from "@darkfactory/ui"

import { OperatorWorkspace } from "../../../components/operator/operator-workspace.civet"

export const metadata = { title: "Operator" }

export default function OperatorPage()
  return (
    <div className="space-y-10">
      <PageHeader
        description="Submit a bounded workflow run, inspect the versioned workflow graph, and review owner-scoped run details from the authenticated operator API."
        eyebrow="Control plane"
        title="Operator workspace"
      />
      <OperatorWorkspace />
  )
