import { PageHeader, SectionHeader } from "@darkfactory/ui"

import { AddressPageClient } from "../../../../components/account/address-page-client.civet"

export default function AddressPage() {
  return (
    <div className="space-y-10">
      <PageHeader description="Maintain account addresses and choose one primary address." eyebrow="Account" title="Addresses" />
      <section aria-labelledby="saved-addresses" className="space-y-6">
        <SectionHeader description="Choose one primary address. Every removal asks for confirmation." id="saved-addresses" title="Saved addresses" />
        <AddressPageClient />
      </section>
    </div>
  )
}
