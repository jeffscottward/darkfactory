import { AuthPanel } from "../../../components/auth/auth-panel.civet"
import { SessionRedirect } from "../../../components/auth/session-redirect.civet"
import { SignUpForm } from "../../../components/auth/sign-up-form.civet"

export const metadata = {
  title: "Create account",
  description: "Create and verify a DarkFactory account.",
}

export default function SignUpPage()
  return (
    <AuthPanel
      description="Create an account, then confirm the email address before signing in."
      eyebrow="New account"
      title="Start with a verified identity."
    >
      <SessionRedirect callbackURL="/dashboard">
        <SignUpForm />
  )
